use std::fmt::Debug;
use core::types::*;
use core::vari::*;

// #[derive(Debug)]
// pub struct Var<'a> {
//     vari: Box<Vari + 'a>,
// }

// impl<'a> Var<'a> {
//     fn new(new_vari: Box<Vari + 'a>) -> Var<'a> {
//         Var { vari: new_vari }
//     }
//     fn val(&self) -> Real {
//         self.vari.val()
//     }

//     fn adj(&self) -> Real {
//         self.vari.adj()
//     }
// }
